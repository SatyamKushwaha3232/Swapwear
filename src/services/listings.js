import { supabase } from "../lib/supabase";
function formatListing(item = {}) { const imageList = Array.isArray(item.images) && item.images.length > 0 ? item.images : item.image ? [item.image] : []; return { id: item.id, title: item.title || "Untitled Item", owner: item.owner_name || "SwapWear User", brand: item.brand || "Unknown Brand", size: item.size || "Free", condition: item.condition || "Good", location: item.location || "India", category: item.category || "Fashion", points: Number(item.points) || 0, likes: Number(item.likes) || 0, views: item.views || "0", hasVideo: Boolean(item.video), video: item.video || "", images: imageList, image: imageList[0] || "", description: item.description || "", user_id: item.user_id || null, created_at: item.created_at || null }; }
async function uploadFile(file, folder) { const fileExt = file.name.split(".").pop(); const fileName = `${folder}/${Date.now()}-${Math.random().toString(36).slice(2)}.${fileExt}`; const { error } = await supabase.storage.from("listings").upload(fileName, file); if (error) throw error; const { data } = supabase.storage.from("listings").getPublicUrl(fileName); return data.publicUrl; }
export async function createListing(data, imageFiles, videoFile) { try { const { data: { user } } = await supabase.auth.getUser(); const safeImages = Array.from(imageFiles || []); const imageUrls = []; for (const file of safeImages) imageUrls.push(await uploadFile(file, "images")); let videoUrl = ""; if (videoFile) videoUrl = await uploadFile(videoFile, "videos"); const { error } = await supabase.from("listings").insert([{ title: data.title, brand: data.brand, category: data.category, size: data.size, condition: data.condition || "Good", location: data.location, points: Number(data.points), description: data.description, image: imageUrls[0] || "", images: imageUrls, video: videoUrl, owner_name: user?.user_metadata?.full_name || "SwapWear User", user_id: user?.id || null }]); if (error) throw error; return { success: true }; } catch (err) { return { success: false, error: err.message }; } }
export async function getListings(userId = null) { try { let query = supabase.from("listings").select("*").order("created_at", { ascending: false }); if (userId) query = query.eq("user_id", userId); const { data, error } = await query; if (error) throw error; return { success: true, data: (data || []).map(formatListing) }; } catch (err) { return { success: false, error: err.message, data: [] }; } }
export async function getListingById(id) { try { const { data, error } = await supabase.from("listings").select("*").eq("id", id).maybeSingle(); if (error) throw error; return { success: true, data: data ? formatListing(data) : null }; } catch (err) { return { success: false, error: err.message, data: null }; } }
export async function deleteListing(id) { try { const { error } = await supabase.from("listings").delete().eq("id", id); if (error) throw error; return { success: true }; } catch (err) { return { success: false, error: err.message }; } }
